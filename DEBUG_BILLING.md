# 🔍 Отладка страницы Billing

## Проверка что страница работает

### 1. Проверьте что бэкенд запущен

```bash
cd backend
npm run dev
```

Должен быть доступен на `http://localhost:3001`

### 2. Проверьте endpoint `/plans`

Откройте в браузере или через curl:

```bash
curl http://localhost:3001/plans
```

Должен вернуть:

```json
{
  "ok": true,
  "plans": [
    {
      "id": "basic",
      "name": "Basic Plan",
      "price": 1000,
      "currency": "KGS"
    },
    ...
  ]
}
```

### 3. Проверьте что вы залогинены как админ организации

1. Войдите в систему: `/b2b/login`
2. Убедитесь что у вас есть организация и вы админ
3. Перейдите на `/b2b/billing` или `/ru/b2b/billing` (в зависимости от локали)

### 4. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:

- Есть ли ошибки в Console?
- Есть ли ошибки в Network tab при загрузке `/plans`?

### 5. Проверьте URL

Правильные URL для страницы billing:

- `/b2b/billing` (английский)
- `/ru/b2b/billing` (русский)
- `/ky/b2b/billing` (кыргызский)

### 6. Возможные проблемы

#### Проблема: Страница пустая или показывает "null"

**Решение:** Проверьте что вы админ организации. Только админы могут видеть страницу billing.

#### Проблема: Ошибка "Failed to load subscription plans"

**Решение:**

1. Проверьте что бэкенд запущен
2. Проверьте что endpoint `/plans` доступен без авторизации
3. Проверьте консоль браузера для деталей ошибки

#### Проблема: Редирект на `/b2b/login`

**Решение:** Вы не залогинены или токен истек. Войдите заново.

#### Проблема: "Only organization administrators can manage billing"

**Решение:** Вы не админ организации. Попросите админа добавить вас как админа или создайте свою организацию.

### 7. Проверка через консоль браузера

Откройте консоль и выполните:

```javascript
// Проверка что API клиент работает
fetch('http://localhost:3001/plans')
  .then((r) => r.json())
  .then(console.log)
```

### 8. Логи бэкенда

Проверьте логи бэкенда при загрузке страницы:

- Должны быть логи `[PAYMENTS] Returning plans:`
- Не должно быть ошибок 401 или 403

## Если ничего не помогает

1. Перезапустите бэкенд: `cd backend && npm run dev`
2. Перезапустите фронтенд: `npm run dev`
3. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
4. Проверьте что все переменные окружения настроены правильно

# 🔧 Объяснение переменных окружения для Finik

## 📋 Что куда вставлять

### Backend (файл `backend/.env`)

```env
# ✅ Эти данные у вас УЖЕ ЕСТЬ от Finik:
FINIK_API_KEY=T3dbQMf3zA6hNV602OKA53iJqcLvWXkC4tauvuHp
FINIK_API_URL=https://api.acquiring.averspay.kg/payment
FINIK_ACCOUNT_ID=3aad26fc-0dcd-4fc8-ac1c-9b6a3d16d523

# ⚠️ FINIK_WEBHOOK_SECRET - что это?
# Это секретный ключ для проверки, что webhook действительно пришел от Finik
#
# Варианты:
# 1. Для локальной разработки - можно использовать любую строку:
FINIK_WEBHOOK_SECRET=dev_secret_12345
#
# 2. После деплоя - получите от Finik в панели управления
#    (они дадут вам этот ключ когда вы настроите webhook URL)

# ⚠️ NEXT_PUBLIC_B2B_URL - что это?
# Это URL вашего ФРОНТЕНДА (Next.js приложение), куда Finik будет редиректить после оплаты
#
# Для локальной разработки:
NEXT_PUBLIC_B2B_URL=http://localhost:3000
#
# После деплоя фронтенда (например на Vercel):
# NEXT_PUBLIC_B2B_URL=https://nuroo-landing.vercel.app
# или
# NEXT_PUBLIC_B2B_URL=https://your-domain.com
```

## 🎯 Пошаговая инструкция

### Шаг 1: Локальная разработка (сейчас)

Добавьте в `backend/.env`:

```env
FINIK_API_KEY=T3dbQMf3zA6hNV602OKA53iJqcLvWXkC4tauvuHp
FINIK_API_URL=https://api.acquiring.averspay.kg/payment
FINIK_ACCOUNT_ID=3aad26fc-0dcd-4fc8-ac1c-9b6a3d16d523
FINIK_WEBHOOK_SECRET=dev_secret_12345
NEXT_PUBLIC_B2B_URL=http://localhost:3000
```

**Важно:**

- `NEXT_PUBLIC_B2B_URL` - это URL где запущен ваш фронтенд (Next.js)
- Если фронтенд на `localhost:3000`, то так и пишите
- Webhook пока не будет работать локально (Finik не достучится до localhost)

### Шаг 2: После деплоя бэкенда

1. **Задеплойте бэкенд** (например на Google Cloud Run, AWS, или другой сервис)
2. **Получите URL бэкенда**, например: `https://api.nuroo.com`

3. **Обновите `backend/.env` на сервере:**

```env
FINIK_API_KEY=T3dbQMf3zA6hNV602OKA53iJqcLvWXkC4tauvuHp
FINIK_API_URL=https://api.acquiring.averspay.kg/payment
FINIK_ACCOUNT_ID=3aad26fc-0dcd-4fc8-ac1c-9b6a3d16d523
FINIK_WEBHOOK_SECRET=ваш_секрет_от_finik  # получите после настройки webhook
NEXT_PUBLIC_B2B_URL=https://your-frontend-domain.com  # URL вашего фронтенда
```

### Шаг 3: Настройка webhook в Finik

1. Зайдите в панель Finik
2. Найдите раздел "Webhooks" или "Настройки"
3. Добавьте webhook URL:

   ```
   https://your-backend-domain.com/webhooks/finik
   ```

   (замените на реальный URL вашего бэкенда)

4. Finik даст вам **webhook secret** - скопируйте его
5. Добавьте этот secret в `FINIK_WEBHOOK_SECRET` в `.env` бэкенда

### Шаг 4: После деплоя фронтенда

1. Задеплойте фронтенд (например на Vercel)
2. Получите URL фронтенда, например: `https://nuroo-landing.vercel.app`
3. Обновите `NEXT_PUBLIC_B2B_URL` в бэкенде:

```env
NEXT_PUBLIC_B2B_URL=https://nuroo-landing.vercel.app
```

## ❓ Частые вопросы

### Q: Что такое `NEXT_PUBLIC_B2B_URL`?

**A:** Это URL вашего фронтенда (Next.js приложение). Finik будет редиректить пользователя на этот URL после оплаты.

### Q: Почему переменная называется `NEXT_PUBLIC_` если она в бэкенде?

**A:** Это историческое название. Она используется бэкендом чтобы знать куда редиректить. Можно было назвать `FRONTEND_URL`, но оставили как есть для совместимости.

### Q: Откуда взять `FINIK_WEBHOOK_SECRET`?

**A:**

- Для локальной разработки: любая строка (например `dev_secret_12345`)
- Для продакшена: получите от Finik в панели управления после настройки webhook URL

### Q: Webhook не работает локально?

**A:** Правильно! Finik не может достучаться до `localhost`. Webhook будет работать только после деплоя бэкенда.

### Q: Где взять URL бэкенда?

**A:** После деплоя бэкенда на облако (Google Cloud, AWS, etc.) вы получите URL типа:

- `https://your-backend.run.app` (Google Cloud Run)
- `https://api.yourdomain.com` (если настроили домен)
- Или другой URL от вашего хостинга

### Q: Где взять URL фронтенда?

**A:** После деплоя фронтенда на Vercel/Netlify/etc:

- `https://your-app.vercel.app` (Vercel)
- `https://yourdomain.com` (если настроили домен)

## 📝 Примеры конфигурации

### Локальная разработка

```env
# backend/.env
FINIK_API_KEY=T3dbQMf3zA6hNV602OKA53iJqcLvWXkC4tauvuHp
FINIK_API_URL=https://api.acquiring.averspay.kg/payment
FINIK_ACCOUNT_ID=3aad26fc-0dcd-4fc8-ac1c-9b6a3d16d523
FINIK_WEBHOOK_SECRET=dev_secret_12345
NEXT_PUBLIC_B2B_URL=http://localhost:3000
```

### Продакшен (пример)

```env
# backend/.env на сервере
FINIK_API_KEY=T3dbQMf3zA6hNV602OKA53iJqcLvWXkC4tauvuHp
FINIK_API_URL=https://api.acquiring.averspay.kg/payment
FINIK_ACCOUNT_ID=3aad26fc-0dcd-4fc8-ac1c-9b6a3d16d523
FINIK_WEBHOOK_SECRET=real_secret_from_finik_dashboard
NEXT_PUBLIC_B2B_URL=https://nuroo-landing.vercel.app
```

## ✅ Чеклист

- [ ] Добавить переменные в `backend/.env` для локальной разработки
- [ ] Задеплоить бэкенд
- [ ] Настроить webhook URL в панели Finik
- [ ] Получить webhook secret от Finik
- [ ] Обновить `FINIK_WEBHOOK_SECRET` в продакшене
- [ ] Задеплоить фронтенд
- [ ] Обновить `NEXT_PUBLIC_B2B_URL` в продакшене на URL фронтенда
